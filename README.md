# Version Inventory / Local Data Workbench

**Локальный инструмент для инвентаризации версий, работы с Excel-данными и подготовки мониторинга**

Версия: **v0.8**

---

## 📋 Описание

Version Inventory — это полностью локальное веб-приложение (без backend), которое помогает:

- 📊 **Управлять инвентарём версий** сервисов, брокеров, монолитов и окружений
- 📁 **Работать с Excel/CSV файлами** — загрузка, парсинг, поиск, фильтрация
- 🔍 **Быстро искать** по всем загруженным данным (глобальный поиск)
- 💾 **Выполнять SQL-запросы** к данным без написания кода
- 📝 **Генерировать команды и скрипты** из шаблонов (curl, bash, ssh, ansible)
- 📈 **Визуализировать данные** через Dashboard с графиками и аналитикой
- 🔧 **Готовить конфигурацию для Zabbix** (скрипты, UserParameters, инвентарь)

**Все данные хранятся локально в браузере** — никаких серверов, никакой отправки данных наружу.

---

## 🚀 Быстрый старт

1. **Откройте `index.html` в браузере**
   - Поддерживаются современные браузеры: Chrome, Firefox, Edge, Safari
   - Рекомендуется Chrome/Edge для лучшей производительности

2. **Загрузите Excel-файл**
   - Перейдите на вкладку **Excel Workspace**
   - Нажмите "Выбрать файл" и загрузите Excel/CSV
   - Поддерживается множественная загрузка файлов

3. **Начните работать**
   - Выберите лист из выпадающего списка
   - Используйте поиск для быстрого нахождения данных
   - Импортируйте данные в инвентарь или используйте SQL-запросы

---

## ✨ Основные возможности

### 📊 Dashboard Analytics
- Обзорная статистика по инвентарю
- Графики распределения версий по окружениям и сервисам
- Выявление аномалий (отстающие версии)
- История изменений версий

### 📁 Excel Workspace
- Множественная загрузка файлов с сохранением в IndexedDB
- Интегрированный глобальный поиск по всем файлам и листам
- Локальный поиск/фильтр по текущему листу
- Выбор колонок и генерация команд по шаблонам
- Импорт данных в инвентарь через маппинг полей

### 🔍 Global Search Engine
- Быстрый поиск по всем загруженным файлам и листам
- Инвертированный индекс для мгновенного поиска
- Переход к найденным строкам одним кликом
- Поиск работает в фоновом потоке (не блокирует UI)

### 💾 SQL / BI Engine
- SQL-подобный язык запросов (SELECT, WHERE, ORDER BY, GROUP BY)
- Агрегации: COUNT, SUM, AVG, MIN, MAX
- JOIN между листами
- Экспорт результатов в CSV/JSON

### 📝 Template Engine
- Создание и управление шаблонами команд/скриптов
- Категории: SSH, CURL, SQL, Ansible, Zabbix и др.
- Применение шаблонов к строкам Excel
- Тестирование шаблонов на примерах
- Сохранение шаблонов для повторного использования

### 🏗️ Inventory Management
- Управление окружениями (Environments)
- Управление хостами (Hosts)
- Управление сервисами (Services)
- Управление эндпоинтами (Endpoints)
- Управление снимками версий (Version Snapshots)
- Version Matrix для сравнения версий по окружениям

### 🔧 Zabbix Agent Builder
- Генерация bash-скриптов для опроса версий
- Генерация UserParameters для Zabbix Agent
- Генерация инвентаря хостов (host;port)
- Пресеты для разных типов сервисов (IIB, монолиты и др.)

---

## 🏗️ Архитектура

### Технологический стек
- **HTML** — одна страница `index.html`
- **CSS** — минималистичный тёмный стиль
- **Vanilla JavaScript (ES6+)** — модульная структура
- **IndexedDB** — хранение больших объёмов данных
- **WebWorkers** — фоновые операции (парсинг, поиск)
- **SheetJS (XLSX)** — парсинг Excel/CSV файлов

### Структура проекта
```
├── index.html              # Главная страница
├── app.js                  # Основная логика приложения
├── style.css               # Стили
├── doc.html                # Полная документация
├── ui-table.js             # UI компонент таблицы
│
├── storage/                # Модуль хранения данных
│   ├── indexeddb.js        # IndexedDB операции
│   ├── constants.js        # Константы
│   └── migration.js         # Миграция данных
│
├── search/                 # Модуль поиска
│   └── search-engine.js     # Интерфейс поиска
│
├── workers/                 # WebWorkers
│   └── search-worker.js     # Worker для поиска
│
├── sql/                     # SQL Engine
│   ├── parser.js            # Парсер SQL-запросов
│   └── query-engine.js      # Движок выполнения запросов
│
├── templates/               # Template Engine
│   └── template-engine.js   # Управление шаблонами
│
└── dashboard/               # Dashboard Analytics
    ├── dashboard-engine.js  # Сбор данных для дашборда
    └── chart-renderer.js    # Рендеринг графиков
```

---

## 📖 Документация

Полная документация доступна в файле **`doc.html`** — откройте его в браузере для подробного описания всех функций, примеров использования и практических сценариев.

### Основные разделы документации:
- Что это за инструмент
- Технологический стек
- Архитектура и модули
- Поток данных и сценарии использования
- Как работать с инструментом
- Интеграция с Zabbix
- Практические сценарии для IIB/монолита

---

## 💡 Примеры использования

### Импорт Excel → Inventory
1. Загрузите Excel-файл в **Excel Workspace**
2. Выберите лист и настройте фильтр
3. Выберите целевую сущность (hosts/services/endpoints)
4. Настройте маппинг полей
5. Нажмите "Импортировать в выбранную сущность"

### Глобальный поиск
1. В **Excel Workspace** введите запрос в поле "поиск по всем файлам и листам"
2. Нажмите "Найти" или Enter
3. Просмотрите результаты, сгруппированные по файлам
4. Нажмите "Перейти" для навигации к найденной строке

### SQL-запросы
1. Перейдите на вкладку **SQL / BI Engine**
2. Выберите исходный лист
3. Введите SQL-запрос, например:
   ```sql
   SELECT HOSTNAME, PORT, ENV 
   FROM sheet 
   WHERE ENV = 'PROD' 
   ORDER BY HOSTNAME
   ```
4. Нажмите "Выполнить"
5. Экспортируйте результаты в CSV/JSON при необходимости

### Генерация скриптов для Zabbix
1. Загрузите Excel с хостами/портами
2. В блоке **Zabbix Builder** выберите:
   - колонку host
   - колонку port
   - item key (например, `iib.version`)
   - путь скрипта (например, `/usr/local/bin/iib_version.sh`)
3. Нажмите "Сформировать Zabbix-скрипт и конфиг"
4. Скопируйте скрипт, UserParameter и инвентарь

---

## 🔒 Безопасность и приватность

- **Все данные хранятся локально** в IndexedDB браузера
- **Никаких серверов** — приложение работает полностью офлайн
- **Никакой отправки данных** — все операции выполняются в браузере
- **Экспорт/импорт данных** — полный контроль над вашими данными

---

## 🛠️ Требования

- Современный браузер с поддержкой:
  - ES6+ (JavaScript)
  - IndexedDB
  - WebWorkers
  - File API

**Рекомендуемые браузеры:**
- Chrome 90+
- Firefox 88+
- Edge 90+
- Safari 14+

---

## 📝 Лицензия

Лицензия будет определена в репозитории.

---

## 🤝 Поддержка

При возникновении проблем или вопросов:
1. Откройте `doc.html` для подробной документации
2. Проверьте консоль браузера (F12) на наличие ошибок
3. Убедитесь, что используете современный браузер

---

**Version Inventory v0.8** — Локальный Data Workbench для инвентаризации версий и подготовки мониторинга.
