# Version Inventory — Standalone Version

**Автономная версия для работы в закрытой среде без интернета**

---

## 📋 Описание

Это **standalone версия** приложения Version Inventory, в которой все внешние зависимости (CDN библиотеки) скачаны и включены локально. Приложение полностью работает **без доступа к интернету**.

---

## 🚀 Быстрый старт

1. **Скопируйте всю папку `standalone/`** на целевую машину
2. **Откройте `index.html` в браузере**
   - Поддерживаются современные браузеры: Chrome, Firefox, Edge, Safari
   - Рекомендуется Chrome/Edge для лучшей производительности

3. **Готово!** Приложение работает полностью офлайн

---

## 📦 Структура

```
standalone/
├── index.html              # Главная страница (с локальными зависимостями)
├── app.js                  # Основная логика приложения
├── style.css               # Стили
├── ui-table.js             # UI компонент таблицы
├── doc.html                # Полная документация
├── README.md               # Этот файл
│
├── vendor/                 # Внешние библиотеки (локальные)
│   └── xlsx.full.min.js    # SheetJS (XLSX) v0.18.5
│
├── storage/                # Модуль хранения данных
│   ├── indexeddb.js
│   ├── constants.js
│   └── migration.js
│
├── search/                 # Модуль поиска
│   └── search-engine.js
│
├── workers/                # WebWorkers
│   └── search-worker.js
│
├── sql/                    # SQL Engine
│   ├── parser.js
│   └── query-engine.js
│
├── templates/              # Template Engine
│   └── template-engine.js
│
└── dashboard/              # Dashboard Analytics
    ├── dashboard-engine.js
    └── chart-renderer.js
```

---

## ✅ Что включено

- ✅ **Все файлы приложения** скопированы
- ✅ **SheetJS (XLSX) v0.18.5** скачан и включен локально
- ✅ **Все CDN ссылки заменены** на локальные пути
- ✅ **Полная автономность** — работает без интернета

---

## 🔒 Безопасность

- Все данные хранятся локально в IndexedDB браузера
- Никаких внешних запросов
- Полная изоляция от сети

---

## 📝 Отличия от основной версии

Единственное отличие — в `index.html` строка:
```html
<!-- Было (основная версия): -->
<script src="https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js"></script>

<!-- Стало (standalone): -->
<script src="vendor/xlsx.full.min.js"></script>
```

Все остальное идентично основной версии.

---

## 🛠️ Требования

- Современный браузер с поддержкой:
  - ES6+ (JavaScript)
  - IndexedDB
  - WebWorkers
  - File API

**Рекомендуемые браузеры:**
- Chrome 90+
- Firefox 88+
- Edge 90+
- Safari 14+

---

## 📖 Документация

Полная документация доступна в файле **`doc.html`** — откройте его в браузере.

---

**Version Inventory v0.8 Standalone** — Полностью автономная версия для закрытых сред.
